version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - pip install --upgrade pip
    build:
      - pip install -r requirements.txt
  env:
    - name: PORT
      value: "8080"
    - name: FLASK_ENV
      value: "production"

run:
  runtime-version: 3.9
  command: python -m gunicorn --bind 0.0.0.0:8080 --workers 4 --timeout 0 "app:app"
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
