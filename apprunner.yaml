version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3 -m pip install --upgrade pip
      - python3 -m pip install pipenv
      - pipenv install
run:
  command: pipenv run python app.py
  network:
    port: 5002
